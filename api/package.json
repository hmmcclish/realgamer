{
  "name": "realgamer-api-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "forever --watch src/index.js",
    "demon": "forever start -a -l /tmp/realgamer-api.log src/index.js",
    "flow": "flow check",
    "lint": "../node_modules/.bin/eslint ."
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "forever": "^0.15.3",
    "hapi": "^16.1.0"
  }
}
